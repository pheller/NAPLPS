<Application xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:vm="using:NAPLPSApp.ViewModels"
             x:Class="NAPLPSApp.App"
             xmlns:local="using:NAPLPSApp"
			 x:DataType="vm:AppViewModel"
			 Name="NAPLPS Toolbox"
             RequestedThemeVariant="Default">
	<Application.DataTemplates>
		<local:ViewLocator/>
	</Application.DataTemplates>

	<Application.Styles>
		<FluentTheme />
		<StyleInclude Source="avares://Avalonia.Controls.DataGrid/Themes/Fluent.xaml"/>
		<!-- AvaloniaEdit's TextEditor is a templated control; without its control theme the
		     Telidraw source pane renders blank no matter what text it holds (the root cause of
		     the empty-source-pane report). -->
		<StyleInclude Source="avares://AvaloniaEdit/Themes/Fluent/AvaloniaEdit.xaml"/>
	</Application.Styles>

	 <NativeMenu.Menu>
    <NativeMenu>
      <NativeMenuItem Header="About NAPLPS Toolbox" Command="{Binding AboutCommand}" />
    </NativeMenu>
  </NativeMenu.Menu>
</Application>